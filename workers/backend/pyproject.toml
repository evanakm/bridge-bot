[project]
name = "bridgebot-cloudflare-api"
version = "0.1.0"
description = "Cloudflare Python Worker API for bridge bot decisions"
requires-python = ">=3.12"
dependencies = []

[dependency-groups]
dev = [
  "workers-py",
  "workers-runtime-sdk",
]
